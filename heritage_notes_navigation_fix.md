# Heritage Notes Navigation Repair

The former `Heritage Notes` destination pointed to `/explore#journal`, which duplicated the Explore experience. It now points to a dedicated protected Traveller route at **`/notes`**.

The new read-only reading room has three editorial pathways that lead back to existing functional content: the Cultural Trail, the live craft collection, and the source-backed Moradabad provenance story. Desktop and mobile navigation both target `/notes`; the dedicated page also has a safe contextual return to Explore.

In an authenticated browser session, `/notes` rendered a distinct reading-room composition and exposed working links to `/planner`, `/explore`, and `/craft/api-105`.

The isolated 390 px mobile capture correctly routed an unauthenticated visitor to the existing responsive traveller demo-login screen, confirming that the new `/notes` page inherits the established protected-route behavior. The mobile menu now includes the dedicated `/notes` target for authenticated Traveller sessions.
